{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "classification.schema.json",
  "title": "Classification",
  "type": "object",
  "required": ["schema_version", "run_id", "input_hash", "classifications"],
  "properties": {
    "schema_version": { "type": "string" },
    "run_id": { "type": "string" },
    "input_hash": { "type": "string" },
    "iteration": { "type": "integer", "minimum": 1, "default": 1 },
    "scope": {
      "type": "object",
      "required": ["root_path", "subtree_path", "path_format"],
      "properties": {
        "root_path": { "type": "string" },
        "subtree_path": { "type": "string" },
        "path_format": { "const": "posix" }
      },
      "additionalProperties": true
    },
    "origin": {
      "type": "object",
      "required": ["root_path", "subtree_path", "path_format"],
      "properties": {
        "root_path": { "type": "string" },
        "subtree_path": { "type": "string" },
        "path_format": { "const": "posix" }
      },
      "additionalProperties": true
    },
    "provenance": {
      "type": "object",
      "properties": {
        "source_artifacts": { "type": "array", "items": { "type": "object" } },
        "derived_from": { "type": "string" },
        "derived_from_hash": { "type": "string" }
      },
      "additionalProperties": true
    },
    "classifications": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["node_id", "category", "recommended_action", "confidence", "rationale"],
        "properties": {
          "node_id": { "type": "string" },
          "purpose": { "type": "string" },
          "category": {
            "enum": ["source", "config", "docs", "cache", "duplicate", "build_artifact", "unknown"]
          },
          "recommended_action": {
            "enum": ["keep", "move", "rename", "delete", "archive", "review"]
          },
          "target_path": { "type": ["string", "null"] },
          "confidence": { "type": "number", "minimum": 0, "maximum": 1 },
          "rationale": { "type": "string" },
          "availability": { "enum": ["available", "cloud_only", "unknown"] }
        }
      }
    }
  }
}
